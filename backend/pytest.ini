[pytest]
# asyncio_mode=auto lets `async def test_*` run without a per-test decorator.
asyncio_mode = auto
# Run from backend/ so `import services...` / `import routers...` resolve.
pythonpath = .
testpaths = tests
